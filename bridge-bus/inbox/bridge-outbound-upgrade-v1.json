{
  "id": "bridge-outbound-upgrade-v1",
  "type": "task_create",
  "created_by": "chatgpt",
  "title": "Bridge outbound payload v1 workspace sync",
  "description": "Infrastructure self-upgrade. Update ONLY these Studio workspace files so they exactly match GitHub main: server/executionPayload.ts, server/githubCommandBus.ts, server/studioRelay.ts. Source of truth is public repo machxanht/BridgeChatgpt main. Do NOT replace package.json or server.ts because the Studio workspace has deployment-specific startup changes. Do NOT alter runtime database or historical task state. After syncing the three files, run npm run lint and npm run build, restart Preview, and verify GET /health returns status ok. This task is only to bring the currently running AI Studio workspace onto the already-reviewed GitHub implementation of ChatGPT->Studio execution payloads. Do not redesign the implementation.",
  "priority": "urgent",
  "assignee": "gemini",
  "related_files": [
    "server/executionPayload.ts",
    "server/githubCommandBus.ts",
    "server/studioRelay.ts"
  ]
}
